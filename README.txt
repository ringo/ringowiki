This is a minimal wiki demo app for RingoJS using Hibernate storage module.

To run it init the DB (MySQL), e.g.:
  mysql -uroot < config/init-mysql.sql

Once that's done launch ringo with the main script:

  ringo main.js

Then point your browser to this URL:

  http://localhost:8080/
